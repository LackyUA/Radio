# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'OnRad.io Connect' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for OnRad.io Connect
  # pod 'Socket.IO-Client-Swift', '~> 13.2.0'
  # pod 'BoringSSL', :podspec => "BoringSSL.podspec"
  pod 'Firebase/Core'
  pod 'Firebase/Messaging'
  pod 'Firebase/Database'
  pod 'Firebase/Firestore'
  pod 'Firebase/Auth'
  pod 'GoogleSignIn'
  pod 'Alamofire', '~> 4.7'
  pod 'SwiftyJSON'
  pod 'Fabric', '~> 1.7.9'
  pod 'Crashlytics', '~> 3.10.4'
  pod 'StreamingKit'
  
  pod 'Kingfisher', '~> 4.0'

  target 'OnRad.io ConnectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'OnRad.io ConnectUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
